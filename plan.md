# plan.md — แผนงานและสถานะ

> อัปเดตล่าสุด: 2026-08-27 — **Phase 0 ปิดรอบ execution แล้ว (gate = FAIL ทั้ง 4 ข้อ, เต็มสเกล)**
> ใช้ไฟล์นี้เป็น **ที่ติดตามงาน** — ถ้ากลับมาทำต่อ อ่าน §3 (ปิด Phase 0 ตามโปรโตคอลที่ล็อกไว้) ก่อนเริ่มโค้ดใด ๆ
> เอกสารหลักยังคงเป็น `target_SomaOS.md` — ถ้าอะไรในไฟล์นี้ขัดกับเอกสารหลัก ให้ยึดเอกสารหลัก

---

## 0. สรุปสั้นที่สุด (อ่านแค่ส่วนนี้ก็พอ)

รอบนี้แก้ไป 4 เรื่อง และเจอของสำคัญ 2 อย่างที่เปลี่ยนความเข้าใจของทั้งโปรเจกต์:

1. **กระดานคะแนนเคยพัง — ซ่อมแล้ว (D-14)** การตอบผ่าน pointer เคยฟรี ตอนนี้ต้องจ่าย token
   ผลคือคะแนนเดิมของ `S` หายไปเกือบหมด (0.515 → 0.018 ใน uniform) เพราะ 92.6% เป็นของปลอม
2. **`S` ดึงของกลับมาไม่เป็น — ซ่อมแล้ว (WP-12)** `long_gap` 0.050 → **1.000** ของจริง (ไม่ใช่ pointer)
3. **เจอเพดานที่แท้จริงของ `S`** — คะแนนของ `S` เท่ากับ "สัดส่วนของ item ที่ query ต้องการ ซึ่งผ่านประตู surprise"
   แทบเป๊ะ 1:1 → **คอขวดอยู่ที่ตอน encode ไม่ใช่ตอน retrieve**
4. **รัน full scale แล้ว (WP-15, 6,336 rows) เจอของใหม่ที่แก้ความเข้าใจเดิม:** `S` **ชนะ** `B2` จริงที่ budget เล็ก
   ในโลกที่ surprise มีสัญญาณ (`long_gap`, `surprise_driven`) แต่ **แพ้ที่ budget ใหญ่** เพราะ store ของ `S`
   ไม่โตตามงบเลย (ตัดสินใจ encode ที่ threshold ตายตัว ไม่ผูกกับ budget) — ดู F8

**gate ไม่ผ่านทั้ง 4 ข้อ ยืนยันแล้วที่ full scale — Phase 0 ปิดรอบ execution ตามโปรโตคอล §7.4/§5**
(ไม่ก้ำกึ่งสักข้อ ไม่ต้องรอ Nin ชี้ขาดว่า "ผ่านไหม" — รายละเอียดที่มาของการปิดอยู่ใน §3)
สิ่งที่เหลือให้ Nin ตัดสินคือ **ทิศทางถัดไป** ไม่ใช่ผลของรอบนี้ — ดู WP-16

**🔴 อัปเดตหลังคุย (F9, §4):** Nin ชี้ว่าเป้าหมายจริงคือความจำแบบสมองคน — external memory เก็บได้ไม่สิ้นสุด
ไม่มีการทิ้งข้อมูลจริง ๆ ฉลาดแค่ตอนเลือกดึงเข้า context ที่จำกัด แต่ `S` ที่ทดสอบไปดัน **ทิ้งข้อมูลออกจาก
store จริง ๆ** ตอน encode ซึ่งขัดกับวิสัยทัศน์นี้โดยตรง — แปลว่ากลไกที่ทดสอบอาจผิดโจทย์ตั้งแต่ต้น
ไม่ใช่แค่ tune ไม่ดี **นี่คือสิ่งที่ควรอ่านก่อนตัดสินใจเรื่องอื่นใน WP-16 ทั้งหมด**

---

## 1. สถานะ

### 1.1 เครื่องมือ Phase 0

- [x] **WP-00 … WP-10** เสร็จหมดตั้งแต่รอบก่อน
- [x] **WP-11** ปิดช่องโหว่ metric (page-fault cost, D-14)
- [x] **WP-12** แก้ retrieval ordering ใน policy `S` (+ ชุบชีวิต `B3`)
- [x] **WP-13** วินิจฉัย KC4 + เพิ่ม regime `surprise_driven` (สำหรับวินิจฉัยเท่านั้น)
- [x] **WP-14** optimize fast path (บางส่วน — ดู §3.4)
- [x] **WP-15** รัน full scale `phase0.json` (6,336 rows, ~10 นาที @ `--jobs 12`)
- [ ] **WP-16** Nin ตัดสินใจ

### 1.2 ผลการทดลอง

- [x] รัน pilot ซ้ำหลังแก้ทั้งหมด (dev seeds เท่านั้น)
- [x] รัน full scale `phase0.json` — 8 regime × 4 budget × 3 tau × 11 seed × 6 policy = 6,336 แถว
- [x] ผ่าน gate Phase 0 — **ไม่ผ่าน ทั้ง 4 ข้อ (KC1–KC4)** ยืนยันด้วยข้อมูลเต็มสเกล ไม่ใช่แค่ pilot อีกต่อไป

**ผลตัดสิน gate (holdout เท่านั้น, ตัวเต็ม):**

| เกณฑ์ | ผล | ค่าที่ได้ | เกณฑ์ที่ต้องการ | n |
|---|---|---|---|---|
| KC1 `S` ชนะ `B2` (ทุก budget รวมกัน) | ❌ FAIL | mean diff −0.3408, 95% CI [−0.3713, −0.3107] | ≥ +0.05 | 576 pairs |
| KC2 competitive_ratio (uniform, budget 4096) | ❌ FAIL | median 0.0242 | ≥ 0.70 | 24 seeds |
| KC3 fast path @ N=10,000 (D-07) | ❌ FAIL | p95 21.37 ms | ≤ 4.0 ms | 7 samples |
| KC4 surprise ↔ ground-truth utility | ❌ FAIL | median ρ 0.0367, 98% CI lower 0.0552 | > 0.25 | 48 traces |

รายงานเต็มอยู่ใน `runs/phase0/report.md` (ไม่ได้ commit เข้า git — `runs/` อยู่ใน `.gitignore` ตามธรรมเนียมเดิม
ตัวเลขสำคัญคัดมาไว้ในไฟล์นี้แล้วเพื่อใช้อ้างอิงได้โดยไม่ต้องรันซ้ำ)

**บั๊กที่เจอระหว่างสร้างรายงาน (แก้แล้ว):** `render_markdown` ใน `report.py` slice `trace_ids` เหลือ 5 ตัว
แต่ลืม slice `config_hashes` — ที่ scale เต็ม (6,336 แถว, `config_hash` เกือบ unique ต่อแถวเพราะรวม seed เข้าไปด้วย)
กลายเป็นบรรทัดเดียวยาว 462 KB ใน markdown แก้แล้วให้โชว์ 5 ตัวแรกทั้งคู่ พร้อมบอกจำนวนรวมและชี้ไปที่ `report.json`
สำหรับ list เต็ม มี regression test คุมไว้ (`test_reproduction_section_stays_bounded_with_many_distinct_config_hashes`)

### 1.3 ⚠️ เหตุการณ์ระหว่างรอบ — ไดรฟ์ D: หลุดกลางงาน

วันที่ 2026-08-26 ระหว่างทำ WP-13/WP-14 ดิสก์ที่เก็บ repo (แยกจากดิสก์ระบบ) หลุดออกจากเครื่องกลางเซสชัน
`git push` ของ WP-11/WP-12 ยังไม่ได้ทำตอนนั้น งาน WP-13/WP-14 ยังไม่ commit เลย
โชคดีที่เสียบกลับมาแล้วข้อมูลอยู่ครบ — แต่เป็นเหตุผลที่ตอนนี้ **push ทันทีทุกจบ WP** แทนที่จะรอจบ session
(บันทึกไว้ใน auto-memory แล้วว่าโปรเจกต์นี้อยู่บนดิสก์แยกที่มีความเสี่ยงนี้)

---

## 2. สิ่งที่ทำเสร็จรอบนี้

### WP-11 — ปิดช่องโหว่ metric ✅

**ปัญหา:** D-13 ให้ตอบผ่าน `source_item_ids` ฟรี → ทิ้งของแล้วเก็บใบเสร็จก็ได้คะแนนเต็ม

**ทางแก้ (D-14):** pointer = page ที่ไม่ resident → ต้อง page fault กลับมา และจ่าย token เท่าขนาด raw

**บทเรียนสำคัญ — ร่างแรกผิด และผิดแบบที่สอนอะไรเยอะ:**
ร่างแรกส่ง `required_item_ids` เข้า resolver แล้ว fault เฉพาะ id ที่ query ต้องการ
= แจก **prefetcher ที่รู้อนาคต** ให้ทุก policy ฟรี ๆ ช่องโหว่ยังเปิดอยู่เหมือนเดิม
(policy ทดสอบที่เก็บแต่ใบเสร็จยังได้ 1.000)

ของจริงคือ **resolver ต้องไม่เห็นเฉลยเลย** — รับแค่ bundle
ลำดับ fault มาจากลำดับที่ policy เขียน pointer ไว้เอง = priority ที่มันประกาศเอง
หลักการเดียวกับที่ `QueryView` ไม่มีฟิลด์ `required_item_ids` ตั้งแต่แรก:
**ทำให้เฉลยรั่วไม่ได้เชิงโครงสร้าง** ไม่ใช่แค่ "ระวังอย่าใช้"

**ผลกระทบ (dev seeds, uniform):**

| budget | ก่อน D-14 | หลัง D-14 |
|---|---|---|
| 1024 | 0.515 | **0.018** |
| 4096 | 0.515 | **0.037** |

metric ใหม่ใน JSONL: `page_faults`, `page_fault_rate`, `page_fault_tokens`,
`page_fault_tokens_per_query`, `answered_via_pointer_rate`, `pointer_denied_rate`,
`effective_tokens_per_query`

### WP-12 — แก้ retrieval ordering ✅

`on_query` แบ่งเป็น 3 รอบ: targeted recall ได้โควตาก่อน → working set เติม → recall เก็บที่เหลือ
เพิ่ม config `recall_budget_fraction` (default **0.25**, tune บน dev seed เท่านั้นตาม D-10)

| regime/budget | 0.00 | 0.10 | **0.25** | 0.50 | 1.00 |
|---|---|---|---|---|---|
| uniform/1024 | 0.009 | 0.044 | **0.063** | 0.063 | 0.063 |
| uniform/4096 | 0.025 | 0.063 | **0.063** | 0.063 | 0.063 |
| long_gap/1024 | 0.050 | 0.523 | **1.000** | 1.000 | 1.000 |
| long_gap/4096 | 0.254 | 1.000 | **1.000** | 1.000 | 1.000 |
| high_noise/1024 | 0.057 | 0.691 | **0.864** | 0.864 | 0.864 |
| variable/1024 | 0.025 | 0.060 | **0.132** | 0.154 | 0.154 |

`long_gap` 1.000 รอบนี้**เป็นของจริง** — required item 163/163 อยู่ใน bundle จริง ไม่มี pointer เกี่ยว

**แถม: `B3` เคยตายมาตลอด** summary ก้อนเดียวโตถึง 10,131 token (ใหญ่กว่า budget ทุกค่า)
→ เข้า bundle ไม่ได้เลย → `B3` ได้คะแนนเท่ากับ `B1` เป๊ะทุกครั้ง
แปลว่า **D-13 ที่สร้างมาเพื่อ `B3` ไม่เคยทำงานให้ `B3` เลยสักครั้ง — มีแต่ `S` ที่ได้ประโยชน์**
แก้ให้ summarize เป็นก้อนย่อย (semantics เดิมตาม D-12) การทำ baseline ให้แข็งขึ้นไม่มีทางเข้าข้าง `S`
มี static warning ใน report.py ว่า D-14 ทำให้ `B3` เป็น **lower bound** เท่านั้น (ดู F7)

### WP-13 — KC4 / benchmark กับ thesis ✅ (วินิจฉัยเสร็จ รอ Nin ตัดสิน)

**ต้นตอที่วัดได้:** ใน `generator.py` การตัดสินว่า observation ไหนเป็น "หลักฐานปัจจุบัน"
เป็นการ**ทอยเหรียญอิสระ** ไม่เกี่ยวกับ surprise เลย
→ observation ที่มายืนยันซ้ำ (surprise 0.02) กลายเป็นเฉลยได้ ทั้งที่โลกไม่มีอะไรเปลี่ยน

**ตัวเลขที่เปลี่ยนความเข้าใจทั้งหมด** — คะแนนของ `S` = สัดส่วน required item ที่ผ่านประตู surprise แทบเป๊ะ:

| regime | ρ (KC4) | surprise เฉลี่ยของ required | %required ที่ ≥ 0.6 | **`S` (4096)** |
|---|---|---|---|---|
| uniform | 0.006 | 0.176 | 6.1% | **0.063** |
| variable | 0.060 | 0.265 | 14.4% | **0.154** |
| topic_drift | 0.056 | 0.256 | 14.2% | — |
| bursty | 0.059 | 0.247 | 14.2% | — |
| `surprise_driven` | 0.215 | 0.795 | 71.3% | **0.707** |
| high_noise | 0.309 | 0.851 | 83.1% | **0.864** |
| long_gap | 0.142 | 1.000 | 100.0% | **1.000** |
| adversarial_flat | −0.006 | 0.490 | 37.2% | — |

→ **คอขวดคือประตูตอน encode ไม่ใช่ตอน retrieve** retrieve เก่งแค่ไหนก็ช่วยไม่ได้ถ้าทิ้งของไปแล้ว

**สิ่งที่ทำ:** เพิ่ม flag `revision_requires_surprise` (default `False` → regime เดิมเหมือนเดิมทุกไบต์)
และ regime ใหม่ `surprise_driven` ที่**ถูกกันออกจากทุก kill criterion** (`DIAGNOSTIC_REGIMES` ใน `gate.py`)
มี test คุมว่า regime นี้ขยับ gate ไม่ได้แม้จะใส่ผลสวยแค่ไหน
เหตุผลทั้งหมดบันทึกใน `plans/CHANGELOG_REGIMES.md`

### WP-14 — ความเร็ว (KC3) ✅ บางส่วน

**เจอบั๊กของ harness:** KC3 **ไม่เคยถูกวัดที่เงื่อนไขที่ D-07 เขียนไว้เลย**
D-07 บอก `N_items = 10,000` แต่ store ของ `S` อิ่มตัวที่ ~308 items ทุก regime
(เพราะ universe มีแค่ `n_entities × n_topics` = 1,440 คู่)
→ ตัวเลข KC3 ทุกตัวก่อนหน้านี้วัดที่ N≈300 ไม่ใช่ N=10,000

**optimize (ผลลัพธ์เหมือนเดิมทุกบิต มี test ยืนยัน):**
- แทน re-sort resident ทุก candidate ด้วย sorted list (bisect) — เดิมกิน 65% ของเวลา
- fuse `extract_features` + `retention_score` เป็น `score_item()` ตัวเดียว

| | ก่อน | หลัง |
|---|---|---|
| `allocate()` @ N=10,000 | 66 ms | **30 ms** |
| KC3 ที่ N=10,000 (harness) | ไม่เคยวัด | **18.35 ms** (เกณฑ์ 4.0) |
| KC3 ที่ scale จริง (~308 items) | 5.13 ms | **0.41 ms** ✅ |

→ **ที่ scale ที่ `S` ทำงานจริง ผ่านสบาย ๆ / ที่ scale ที่ D-07 กำหนด ยังไม่ผ่าน**
รายงานทั้งสองตัวเลขในทุก report ตัดสิน gate ด้วยตัวที่ D-07 กำหนด

## 3. สิ่งที่ต้องทำต่อ

### WP-15 — รันเต็มสเกล ✅

รัน `phase0.json` เต็ม (8 regime × 4 budget × 3 tau × 11 seed × 6 policy = 6,336 แถว) เสร็จใน ~10 นาที
ด้วย `--jobs 12` (คำนวณเวลาก่อนจาก pilot calibration: 720 แถว/100s → ประเมิน ~20-25 นาที จริงเร็วกว่าที่คาด)
วัด holdout ครั้งเดียว — ไม่มีการ tune ใด ๆ หลังเห็นผล `recall_budget_fraction` ล็อกไว้ที่ 0.25 จาก WP-12 แล้ว

**เจอของใหม่ที่สำคัญมาก — ต้องแก้ข้อ 4 ของสรุปสั้นและ F6 เดิม:**

ตอนวิเคราะห์แค่ dev seed 2 ตัวที่ budget=4096 อย่างเดียว (รอบก่อน) สรุปว่า `B2` ชนะ `S` "เกือบทุกที่"
แต่พอดู full scale แยกตาม budget จริง ๆ (holdout, ทุก tau เฉลี่ย) ภาพต่างไปมาก:

| regime | budget | `S` | `B2` | `S − B2` |
|---|---|---|---|---|
| surprise_driven | 1024 | 0.736 | 0.332 | **+0.404** |
| surprise_driven | 2048 | 0.736 | 0.544 | **+0.192** |
| surprise_driven | 4096 | 0.736 | 0.789 | −0.053 |
| surprise_driven | 8192 | 0.736 | 0.947 | −0.211 |
| long_gap | 1024 | 0.983 | 0.424 | **+0.559** |
| long_gap | 2048 | 0.983 | 0.645 | **+0.338** |
| long_gap | 4096 | 0.983 | 0.877 | +0.106 |
| long_gap | 8192 | 0.983 | 0.993 | −0.010 |
| uniform | 1024 | 0.025 | 0.244 | −0.219 |
| uniform | 8192 | 0.026 | 0.914 | −0.888 |

**`S` ชนะ `B2` จริง ๆ ที่ budget เล็ก ในโลกที่ surprise มีสัญญาณ** (`long_gap`, `surprise_driven`)
แต่พอ budget โต `B2` ไล่ทันแล้วแซง เพราะ **`strict_recall` ของ `S` แทบไม่ขยับตาม budget เลย** (0.736 คงที่ทุก budget)
ในขณะที่ `B2` ไต่ขึ้นเรื่อย ๆ แบบเกือบเป็นเส้นตรงกับ budget

→ ดู **F8** ด้านล่าง สำหรับสาเหตุ (encode ไม่ผูกกับ budget เลย)

### WP-16 — Nin ตัดสินใจ 🔵 (agent ตัดสินแทนไม่ได้)

**หมายเหตุ: full scale (WP-15) แก้ตัวเลขของคำถามที่ 2 จากรอบก่อน — อ่าน F8 ก่อนตอบ**

#### ❓ คำถามที่ 0 (ใหม่ที่สุด — น่าจะต้องตอบก่อนข้ออื่น) — ดู **F9** ก่อน
Nin อธิบายวิสัยทัศน์จริงเพิ่มเติมหลังอ่านผลรอบนี้: เป้าหมายคือความจำแบบสมองคน (local context ที่ใช้งานอยู่
+ external memory ไม่สิ้นสุดที่ดึงกลับมาได้) ไม่ใช่แค่ "จำ/ลืม" — ซึ่งชี้ว่า `S` ที่ทดสอบไปอาจ**ผิดโจทย์ตั้งแต่ต้น**
เพราะมันทิ้งข้อมูลออกจาก store จริง ๆ ตอน encode แทนที่จะเก็บทุกอย่างไว้แล้วฉลาดแค่ตอนดึงเข้า context
รายละเอียดเต็มอยู่ที่ F9 (§4) — **คำตอบของคำถามนี้อาจทำให้คำถามที่ 2 และ 4 ด้านล่างไม่จำเป็นอีกต่อไป**
(ถ้าตัดสินใจว่าโจทย์จริงคือ F9 การแก้ `S` ตามคำถาม 4 แบบเดิมอาจไม่ตรงจุดอีกแล้ว ต้องออกแบบ policy ใหม่แทน)

#### ❓ คำถามที่ 1 — KC4: ข้อสอบผิด หรือ thesis ผิด
- **(ก)** workload ที่ SomaOS ตั้งใจจะเก่ง = แบบ `surprise_driven`
  → regime เดิมออกแบบไม่ตรงกับสิ่งที่จะทดสอบ ต้อง **pre-register ชุดใหม่ก่อนดูผล**
- **(ข)** "ถามหาหลักฐานล่าสุด" เป็น workload จริงและสำคัญ
  → KC4 เป็นจริง สมมติฐานผิดสำหรับ workload กลุ่มนี้ ต้องรายงานตรง ๆ ตาม §7.4

**ข้อมูลใหม่ที่ทำให้ตัดสินยากขึ้น:** แม้แต่ `surprise_driven` เอง (ที่สร้างมาให้สมมติฐานเป็นจริง)
ก็ยังได้ ρ เฉลี่ยแค่ **0.1908** (n=132) — **ต่ำกว่าเกณฑ์ 0.25 อยู่ดี**
แปลว่าการผูก revision กับ surprise เพียงอย่างเดียว **ไม่พอ** ที่จะทำให้ KC4 ผ่าน
ต้องพิจารณาว่านี่คือสัญญาณว่า (ก) ยังไม่ใช่คำตอบเต็ม หรือ (ข) มีน้ำหนักมากกว่าที่คิด

#### ❓ คำถามที่ 2 — benchmark นี้พิสูจน์ thesis ได้จริงไหม (ดู §4 F6, F8 — **อัปเดตจาก full scale**)
รอบก่อนสรุปจาก dev seed 2 ตัวที่ budget เดียวว่า "`B2` ชนะเกือบทุกที่" — **full scale แสดงภาพที่ต่างออกไป**
`S` ชนะ `B2` จริงที่ budget เล็กในโลกที่ surprise มีสัญญาณ (`long_gap` +0.56 ที่ budget 1024, `surprise_driven` +0.40)
แต่แพ้ที่ budget ใหญ่เพราะ **`S` ไม่ใช้ budget ที่เพิ่มขึ้นเลย** (F8) — เป็นบั๊กเชิงออกแบบที่ต่างจาก "cost model เข้าข้าง B2"
ที่เข้าใจไว้รอบก่อน คำถามตอนนี้คือ: F8 ควรถูกนับเป็นส่วนหนึ่งของ Phase 0 (แก้แล้ววัดใหม่) หรือเป็นข้อสังเกตสำหรับ Phase 1?

#### ❓ คำถามที่ 3 — KC3 จะเอายังไงต่อ
- ยอมรับว่าไม่ผ่านที่ N=10,000 (pure Python ทำ 4 ms ไม่ได้จริง ๆ — วัดจริงที่ full scale ได้ 21.37 ms)
- หรืออนุมัติให้แก้ contract §4 เพื่อทำ numpy vectorization แบบ array ถาวร (งานใหญ่ กระทบ interface)
- หรือทบทวนว่า N=10,000 เป็นเงื่อนไขที่สมเหตุสมผลไหม ในเมื่อ workload จริงอิ่มตัวที่ ~300-1000

#### ❓ คำถามที่ 4 (ใหม่) — F8 ควรแก้ไหม ถ้าแก้ ต้องขออนุมัติก่อนเหมือน D-13→D-14
`S` เข้ารหัส (encode) item ด้วย threshold คงที่ (`tau_high=0.6`) ไม่สนใจว่า budget เหลือเท่าไหร่
ถ้าให้ store โตตาม budget ได้ (เช่น threshold ผ่อนลงเมื่อ budget สูง) น่าจะทำให้ `S` ไม่ plateau ที่ budget ใหญ่
**แต่นี่คือการเปลี่ยนพฤติกรรม policy หลังเห็นผลว่ามันแพ้ที่ budget ใหญ่ — เข้าข่าย §15.7 ถ้าทำไม่ระวัง**
ต้องแยกให้ชัดว่า:
- ถ้าทำ = ปรับ **architecture** ของ `S` (เพิ่มความสามารถใหม่ ไม่ใช่ tune parameter เดิม) → อาจนับเป็น WP ใหม่ได้
- แต่ต้อง **pre-register บน dev ก่อน วัด holdout ครั้งเดียว** เหมือน WP-12 ทุกประการ ห้ามดู holdout ระหว่างออกแบบ

---

### ปิด Phase 0 ตามโปรโตคอลที่ล็อกไว้ (master plan §5, `target_SomaOS.md` §7.4)

> `plans/00_PHASE0_MASTER_PLAN.md` §5 เขียนไว้ตรง ๆ ตั้งแต่ต้นทาง:
> "ไม่ผ่าน → หยุด ตาม §7.4 ... **ห้าม Sonnet ตัดสิน gate เอง ห้ามปรับ weight/config เพื่อให้ผ่าน**
> ถ้าตัวเลขไม่ผ่าน → รายงานตัวเลขนั้น พร้อม hypothesis ว่าทำไม แล้วหยุด"

ตัวเลขจาก WP-15 (full scale, holdout, วัดครั้งเดียว) fail ทั้ง 4 เกณฑ์แบบไม่ก้ำกึ่งเลยสักข้อ
(KC1 diff −0.34 ห่างจากเกณฑ์ +0.05 มาก, KC2 อยู่ที่ 3% ของเกณฑ์, KC3 เกินเกณฑ์ 5 เท่า, KC4 อยู่ที่ 15% ของเกณฑ์)
→ **นี่ไม่ใช่กรณีก้ำกึ่งที่ต้องรอ Nin ชี้ขาดว่า "ผ่านไหม"** ตัวเลขชี้ขาดเองอยู่แล้วตามเกณฑ์ที่ pre-register ไว้ก่อนเห็นผล

ดังนั้นส่วนที่ **executable ได้เองตามโปรโตคอล** (ไม่ต้องรอ Nin) คือ:
- [x] **ประกาศ gate = FAIL** ตามตัวเลข ไม่ปรับอะไรให้ผ่าน
- [x] **หยุด** งานเชิงฝีมือของ Phase 0 ที่นี่ — ไม่ implement คำถามที่ 4 (แก้ F8), ไม่ทำ numpy rewrite ของ KC3,
      ไม่แก้ regime เพิ่มเพื่อไล่ KC4 การทำสิ่งเหล่านี้ตอนนี้ = "ฝืนทำต่อ" ที่ §7.4 เตือนไว้ตรง ๆ
- [x] **บันทึกสิ่งที่เรียนรู้** (ด้านล่าง) — ตาม master plan "เสียเวลาสองสัปดาห์ ดีกว่าเสียหกเดือน"

ส่วนที่ **ยังเป็นของ Nin โดยเฉพาะ ไม่ใช่สิ่งที่ agent ตัดสินแทนได้** (ต่างจากการอ่านตัวเลข):
- จะกลับมาแก้ F8 / ทำ Phase 0 รอบใหม่ด้วย regime ที่ pre-register ใหม่ (คำถาม 1, 4) หรือไม่
- จะยอมรับว่า thesis ไม่ได้รับการสนับสนุนจาก benchmark นี้ แล้วหยุดที่นี่เลย (§7.4 เต็มรูปแบบ) หรือไม่
- จะลงทุนแก้ KC3 ด้วย numpy (งานเปลี่ยน contract, ไม่ใช่งานเล็ก) หรือไม่ (คำถาม 3)

คำถามพวกนี้ **ไม่ได้ถูกตอบในรอบนี้** เพราะการตอบ = ตัดสินทิศทางของโปรเจกต์ ซึ่งอยู่นอกเหนือขอบเขตที่มอบให้ agent
`CLAUDE.md` และ `plans/00_PHASE0_MASTER_PLAN.md` §5 ยังคงมีผลเหมือนเดิม — สโคปยังจำกัดแค่ `broker/`, `bench/`, `tests/`
ห้ามสร้าง `kernel/`, `registry/`, `cortex/` ฯลฯ **จนกว่า Nin จะสั่งเปิด phase ถัดไปอย่างชัดเจน**

### สิ่งที่เรียนรู้จาก Phase 0 (มีค่ากว่าการฝืนทำต่อ — master plan §5)

1. **แนวคิด "deterministic policy ตัดสินใจแทน LLM" วัดผลได้จริง** — เครื่องมือทั้งชุด (retention scoring,
   working-set allocator, OPT oracle, kill-criteria gate) ทำงานถูกต้อง มี test คุม 360 เคส, ยืนยัน `exact_belady`
   กับ brute force 30/30 seed, B0 sanity gate ผ่านทุก regime — **โครงสร้างพิสูจน์แนวคิดใช้งานได้จริง แม้ตัวแนวคิดเองจะยังไม่ผ่าน**
2. **metric ที่ดูดีเกินจริงคือสัญญาณอันตราย ไม่ใช่ข่าวดี** — D-13 ให้ `S` ได้ 1.000 แบบปลอมจากการนับ pointer ฟรี
   ถ้าไม่จับได้ตอนนั้น รอบนี้จะรายงานผลผ่านทั้งที่กระดานคะแนนพัง บทเรียน: **ทุกครั้งที่ตัวเลขดีเกินคาด ต้องสงสัยก่อนเชื่อ**
3. **คอขวดจริงของ `S` อยู่ที่ "จะจำอะไร" (encode) ไม่ใช่ "จะค้นอะไร" (retrieve)** — งาน WP-12 ทำให้ retrieval
   ดีขึ้นจริง (`long_gap` 0.05→1.00) แต่แทบไม่ขยับ `uniform` เลย (0.06) เพราะของถูกทิ้งตั้งแต่ต้นทาง
   ยืนยันด้วย WP-13: คะแนนของ `S` ผูกกับ %required ที่ผ่านประตู surprise แทบเป๊ะ 1:1 ทุก regime
4. **"ลืมอย่างฉลาด" ต้องพิสูจน์ว่าคุ้มค่าจริง ไม่ใช่แค่ไม่เสียหาย** — ใน benchmark นี้ B2 (จำทุกอย่าง) ไม่เสีย
   อะไรเลยจากการจำเยอะ (D-06 คิดต้นทุนแค่ตอน query) ทำให้ "ความเลือกสรร" ของ `S` มีค่าเฉพาะตอน budget จำกัดจริง ๆ
   (F8) — เป็นเงื่อนไขที่ thesis ต้องระบุให้ชัดว่า "ได้เปรียบเมื่อไหร่" ไม่ใช่ "ได้เปรียบเสมอ"
5. **regime ที่สร้างมาให้สมมติฐานเป็นจริงที่สุด ก็ยังไม่พอ** — `surprise_driven` (ผูก revision กับ surprise
   ตรง ๆ ตามนิยาม D-04) ได้ ρ แค่ 0.19 ไม่ถึง 0.25 แปลว่าช่องว่างระหว่าง "surprise สูง" กับ "สำคัญต่อการตอบคำถาม"
   ไม่ได้ปิดง่าย ๆ ด้วยการปรับ generator อย่างเดียว — อาจเป็นข้อจำกัดของนิยาม surprise เองที่ D-04 ใช้
6. **ระเบียบวิธี (dev/holdout split, pre-registration, กันรัน regime ใหม่ไม่ให้ขยับ gate) ทำงานได้ตามที่ตั้งใจ**
   ตลอด 2 รอบไม่มีครั้งไหนที่ตัวเลขบน holdout ถูกดูก่อนตัดสินใจ tune และ `surprise_driven` ที่เพิ่มเข้ามาก็ไม่เคย
   ได้สิทธิ์ขยับ gate แม้จะให้ผลดีกว่า — วินัยนี้เองเป็นสิ่งที่ทำให้ผล FAIL รอบนี้ **น่าเชื่อถือ**

## 4. ข้อค้นพบที่ต้องไม่ลืม

### F6 — benchmark อาจตอบคำถามที่มันถูกสร้างมาเพื่อถามไม่ได้ ⚠️ สำคัญ (แก้ไขจากรอบก่อนด้วย full-scale data — ดู F8)

**⚠️ แก้ไข:** เวอร์ชันแรกของ F6 (เขียนจาก dev seed 2 ตัว, budget เดียว) สรุปว่า "`B2` ชนะ `S` แทบทุกที่แม้ surprise
ชี้เป้าถูก" — full scale (WP-15) แสดงว่า **ไม่จริงทั้งหมด**: ที่ budget เล็ก `S` ชนะ `B2` จริงในโลกที่ surprise
มีสัญญาณ (ดู F8) เหตุผลที่ `B2` แซงตอน budget ใหญ่**ไม่ใช่**เพราะ cost model เข้าข้าง B2 ล้วน ๆ อย่างที่เข้าใจไว้เดิม
แต่ส่วนใหญ่มาจาก F8 (`S` ไม่ใช้ budget ที่เพิ่มขึ้นเลย) — ยังคงมีองค์ประกอบของ D-06 อยู่บ้าง (ดูด้านล่าง)
แต่ไม่ใช่คำอธิบายหลักอีกต่อไป

ส่วนที่ยังจริงอยู่ (เป็นเหตุผลเชิงโครงสร้าง ไม่ใช่ tune ไม่ดี):
- D-06 นิยาม cost = token ใน bundle ตอน query เท่านั้น
- `B2` เก็บทุก observation ไว้ไม่จำกัด **โดยไม่เสียค่าอะไรเลย** แล้วค้นด้วย tag ที่เป็น ground truth
- ที่ budget ใหญ่พอ `B2` ยัดเกือบทุกอย่างที่เกี่ยวข้องได้ ในขณะที่ `S` มี "เพดานของตัวเอง" ที่ต่ำกว่า budget มาก

นี่คือสิ่งที่ §3.2 ของ master plan เตือนไว้เองเรื่อง "self-validation in a vacuum"
แต่ตอนนี้รู้แล้วว่าเพดานของ `S` ส่วนใหญ่มาจาก F8 ไม่ใช่จาก cost model เพียงอย่างเดียว

**ย้ำ: นี่ไม่ใช่ข้อแก้ตัว** KC1 fail = fail ต้องรายงานตามนั้น
แต่ต้องรายงานคู่กันด้วยว่า *การทดลองนี้มีขอบเขตแค่ไหน* ไม่งั้นจะสรุปผิดทั้งสองทาง
(ทั้ง "SomaOS ใช้ไม่ได้" และ "แค่ tune อีกหน่อยก็ชนะ")

### F7 — `B3` เป็น lower bound ไม่ใช่ baseline ที่แข่งจริง

D-14 ทำให้ `B3` **ไม่มีทางได้ประโยชน์จากการ summarize** เพราะ answerability เทียบ id ตรง ๆ (D-02)
ไม่มีแนวคิดว่า "summary เก็บใจความไว้ได้บางส่วน" → มีแต่เสีย token
"`S` ชนะ `B3`" จึงแทบไม่ใช่หลักฐานอะไร — ตัวที่ต้องชนะคือ `B2` (ตาม KC1 อยู่แล้ว)
มี static warning ขึ้นทุก report

---
### F8 — `S` ไม่ใช้ budget ที่เพิ่มขึ้นเลย: encode ตัดขาดจาก budget โดยสิ้นเชิง ✅ ยืนยันแล้ว (ใหม่ จาก WP-15)

ตัวเลขที่ชี้ชัด (holdout, ทุก tau เฉลี่ย):

```
regime=surprise_driven   S ที่ budget 1024/2048/4096/8192 = 0.736 / 0.736 / 0.736 / 0.736   <- คงที่เป๊ะ
regime=long_gap          S ที่ budget 1024/2048/4096/8192 = 0.983 / 0.983 / 0.983 / 0.983   <- คงที่เป๊ะ
```

ในขณะที่ `B2` ไต่ขึ้นเกือบเป็นเส้นตรงกับ budget ทุก regime (เช่น uniform: 0.244 → 0.427 → 0.710 → 0.914)

**สาเหตุ:** `SomaPolicy.observe()` ตัดสิน encode ด้วย `item.surprise >= self.tau_high` (ค่าคงที่ 0.6)
เท่านั้น **ไม่เคยดู `self._budget_tokens` เลย** ดังนั้นไม่ว่า budget จะเป็น 1024 หรือ 8192
`S` เก็บ item ชุดเดียวกันเป๊ะ ๆ เข้า store (ยืนยันจาก WP-14: store อิ่มตัวที่ ~308-1028 item ไม่ขึ้นกับ budget)
พอ store ไม่โต จำนวนคำตอบที่เป็นไปได้สูงสุดก็คงที่ตาม — เพิ่ม budget ให้เท่าไหร่ก็ไม่ช่วย

**ผลตามมา:** ที่ budget เล็ก `S` ได้เปรียบเพราะ selective กว่า `B2` มาก (เก็บน้อยแต่ตรง)
พอ budget โตพอที่ `B2` จะ "ยัดเกือบทุกอย่าง" ได้ `S` ก็โดนแซงเพราะไม่มีกลไกให้เก็บเพิ่ม

**นี่คือ finding ไม่ใช่ bug ที่ควรรีบแก้เงียบ ๆ** — เก็บไว้เป็นคำถามที่ 4 ใน WP-16 (§3) ให้ Nin ตัดสินว่า
ควรแก้ในสโคปนี้ (แล้ว pre-register บน dev ใหม่) หรือปล่อยเป็นข้อสังเกตสำหรับ Phase 1

### F9 — 🔴 สิ่งที่ทดสอบไปอาจเบี่ยงจากวิสัยทัศน์จริงของ SomaOS (จาก Nin, 2026-08-27 — สำคัญที่สุดในเอกสารนี้)

Nin อธิบายเป้าหมายจริงของโปรเจกต์ให้ชัดขึ้นหลังอ่านผลรอบนี้ สรุปคือ:

> ไม่ใช่เรื่อง "จำอะไร ลืมอะไร" — ต้องการให้เหมือนสมองคน: ส่วนที่ใช้งานอยู่ = **local memory (context)**
> ส่วนที่เป็นความทรงจำ/ประสบการณ์/ข้อมูลที่ไม่ได้ใช้ตอนนี้ = **external memory** ที่มีโครงสร้างดึงกลับมาได้ง่าย
> เป้าหมาย: ไม่ให้ agent แบก context เยอะเกินไป **ไม่ใช่ให้ข้อมูลหายไป**
> เป้าหมายระยะยาว: ความจำไม่สิ้นสุด เหมือนคน — เก็บได้ไม่จำกัด แค่ไม่ปนกับ context ที่ใช้งานอยู่

**นี่คือจุดที่กลไกที่ทดสอบไปเบี่ยงจากวิสัยทัศน์นี้จริง ๆ:**

Cost model ของ Phase 0 (D-06) คิดต้นทุนแค่ token ที่อยู่ใน context ตอน query เท่านั้น **ไม่คิดค่าคลังภายนอกเลย**
ซึ่งตรงกับวิสัยทัศน์ของ Nin อยู่แล้ว (คลังเก็บได้ไม่จำกัดไม่เสียอะไร) — ส่วนนี้ไม่ผิด

**แต่ policy `S` ที่ออกแบบมาทดสอบ ดันไป "ทิ้งข้อมูลจริง ๆ" ตั้งแต่ตอน `observe()`:**
ถ้า item ไม่ผ่านประตู surprise (`tau_high`) จะไม่ถูกเก็บเข้า **store** เลยด้วยซ้ำ (ไม่ใช่แค่ไม่เอาเข้า context)
= ไม่มีทางดึงกลับมาได้อีกเลยตลอดกาล ตรงข้ามกับ "external memory ไม่สิ้นสุด" ที่ Nin ต้องการ

ในขณะที่ `B2` (ที่ชนะในหลาย regime) มีรูปร่างที่ **ใกล้เคียงวิสัยทัศน์ของ Nin มากกว่า**: เก็บทุก observation
ไว้ในคลังแบบไม่จำกัด (= external memory ไม่สิ้นสุด) แล้วดึงเฉพาะที่เกี่ยวข้องเข้า context ตอน query
(แม้วิธีดึงของ `B2` เองจะยังหยาบมาก — ใช้ ground-truth tag ตรง ๆ ไม่ใช่ความเข้าใจจริง ดู F6)

**สรุป:** ผล FAIL ของรอบนี้ **ไม่ได้แปลว่าวิสัยทัศน์ "ความจำไม่สิ้นสุดแบบสมองคน" ผิด**
มันแปลว่า **กลไกที่เอาไปทดสอบ (`S` ที่ทิ้งข้อมูลตั้งแต่ประตูแรก) ขัดกับวิสัยทัศน์นั้นเอง**
โจทย์ที่ตรงกับสิ่งที่ Nin ต้องการจริง ๆ ควรเป็น:

> เก็บทุกอย่างไว้ใน external memory แบบไม่มีทิ้ง (เหมือนโครงร่างของ `B2`)
> แล้วให้ policy ฉลาดขึ้น **เฉพาะตอนเลือกดึงอะไรเข้า context ที่จำกัด** — จะดีกว่าการค้นด้วยความคล้ายแบบหยาบ ๆ ไหม
> (และอาจใช้ "ความน่าประหลาดใจ" เป็นสัญญาณช่วยจัดอันดับตอนดึง แทนที่จะใช้เป็นเงื่อนไขว่าจะเก็บหรือทิ้งตั้งแต่แรก)

**นี่คือคำถามที่ต่างจาก F8 และกว้างกว่าคำถามที่ 2 ของ WP-16 เดิม** — F8 ถามว่า "ควรแก้ให้ `S` เก็บมากขึ้นตาม budget ไหม"
แต่ F9 ถามลึกกว่านั้นว่า **"`S` ไม่ควรทิ้งข้อมูลออกจาก external memory เลยตั้งแต่แรก"** ซึ่งเป็นการออกแบบ
policy คนละแบบไปเลย ไม่ใช่แค่การ tune threshold

**ไม่เปลี่ยนผลตัดสิน Phase 0** — เกณฑ์ที่ pre-register ไว้ก่อนรันยังคง FAIL ตามที่วัดได้ (§3 ยังปิดตามเดิม)
F9 เป็นบริบทสำหรับตัดสินใจว่าจะทำอะไรต่อ ไม่ใช่เหตุผลให้กลับไปแก้ตัวเลขของรอบนี้

## 5. กฎที่ต้องรักษาตลอด (ย้ำจากเอกสารหลัก)

- ❌ **ห้ามแก้ threshold / weight / regime config เพื่อให้ผ่าน gate** (§15.7)
- ❌ **ห้าม tune แล้ววัดบน holdout ซ้ำ ๆ** — tune บน dev, วัด holdout ครั้งเดียว (D-10)
- ❌ **ห้ามสร้าง** `kernel/`, `registry/`, `cortex/`, `modelbus/`, `trace/`, `packs/` (§12)
- ❌ ห้ามเพิ่ม dependency นอก stdlib + numpy โดยไม่ถาม
- ⚠️ แก้ decision ที่ล็อกแล้ว (D-01..D-14) ต้องขออนุมัติก่อน ห้ามแก้เงียบ ๆ
- ⚠️ regime ที่เพิ่มหลังเห็นผล **ต้องอยู่ใน `DIAGNOSTIC_REGIMES`** ห้ามให้ขยับ gate
- ⚠️ **push ทันทีที่จบแต่ละ WP** — อย่ารอสะสม (ดู §1.3, บทเรียนจากรอบนี้)
- 🚨 สัญญาณว่า harness พัง ไม่ใช่ policy เก่ง: `competitive_ratio > 1.0`, `B0 < 1.0`,
  `S` ชนะใน `adversarial_flat`, `answered_via_pointer_rate` สูงผิดปกติ,
  หรือ test ผ่านหลังแก้ config ในคอมมิตเดียวกัน
