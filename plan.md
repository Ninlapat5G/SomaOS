# plan.md — แผนงานรอบถัดไป

> อัปเดตล่าสุด: 2026-08-26
> ใช้ไฟล์นี้เป็น **ที่ติดตามงาน** — รอบหน้ามาเปิดไฟล์นี้ก่อน แล้วทำต่อจากช่องที่ยังไม่ติ๊ก
> เอกสารหลักยังคงเป็น `target_SomaOS.md` — ถ้าอะไรในไฟล์นี้ขัดกับเอกสารหลัก ให้ยึดเอกสารหลัก

---

## 1. สถานะตอนนี้

### 1.1 เครื่องมือ Phase 0 — เสร็จหมดแล้ว

- [x] **WP-00** โครงโปรเจกต์ + seeded RNG + canonical hashing
- [x] **WP-01** data contract (MemoryItem, Query/QueryView, ContextBundle)
- [x] **WP-02** ตัวสร้าง trace จำลอง 7 regime + world model
- [x] **WP-03** เครื่องคิดคะแนน retention (pure function, coverage 98%)
- [x] **WP-04** ตัวจัดสรร working set + hysteresis + eviction log (91%)
- [x] **WP-05** policy protocol + baseline B0–B4
- [x] **WP-06** policy `S` (surprise gating + working set + targeted recall)
- [x] **WP-07** OPT oracle (`exact_belady` + `upper_bound` + brute-force validation) (96%)
- [x] **WP-08** metrics + runner (deterministic, ขนานได้, B0 sanity gate)
- [x] **WP-09** kill-criteria gate + report generator
- [x] **WP-10** determinism / layering / invariant tests + `scripts/ci.sh`
- [x] รวม **323 tests ผ่านหมด**, push ขึ้น GitHub แล้ว

### 1.2 ผลการทดลอง — ยังไม่ผ่าน

- [x] รัน pilot scale (5 regime × 2 budget × 10 seed × 2000 ticks = 600 rows)
- [ ] รัน full scale ตาม `phase0.json` — **ยังไม่ได้ทำ**
- [ ] ผ่าน gate Phase 0 — **ยังไม่ผ่าน (FAIL ทั้ง 4 ข้อ)**

| เกณฑ์ | ผล | ค่าที่ได้ | เกณฑ์ที่ต้องการ |
|---|---|---|---|
| KC1 `S` ชนะ `B2` | ❌ FAIL | −0.4958 (แพ้) | ≥ +0.05 |
| KC2 competitive_ratio | ❌ FAIL | 0.4770 | ≥ 0.70 |
| KC3 fast path ≤ 5% | ❌ FAIL | 5.13 ms | ≤ 4.0 ms |
| KC4 surprise สัมพันธ์กับความสำคัญ | ❌ FAIL | ρ = 0.1056 | > 0.25 |

---

## 2. ผลวิเคราะห์ (จากการวินิจฉัยด้วยข้อมูลจริง ไม่ใช่การเดา)

### F1 — `S` เก็บของครบ แต่ดึงกลับมาไม่เป็น ✅ ยืนยันแล้ว

วินิจฉัยบน `uniform / h-01 / budget 4096`:

```
required item อยู่ใน store        : 157/157  (100.0%)   <- เก็บครบ ไม่ใช่ปัญหา
required item เข้า bundle จริง    :  80/157  ( 51.0%)   <- ปัญหาอยู่ตรงนี้
สัดส่วน token ที่ working set กิน :            99.9%    <- ต้นตอ
```

**สาเหตุ:** `on_query` ใน `s_soma.py` เติม bundle จาก working set **ก่อน** จนงบหมด
แล้วค่อยเหลือที่ให้ targeted recall — ซึ่งแทบไม่เหลือเลย
แต่ working set ถูกเลือกจาก "เป้าหมายย้อนหลัง" (topic ที่เพิ่งเจอบ่อย) ซึ่ง**ไม่เกี่ยวกับคำถามที่กำลังถาม**

ทดสอบทางเลือก (บน dev seed เท่านั้น ตาม D-10) — กันงบส่วนหนึ่งให้ targeted recall ก่อน:

| recall_frac | uniform | long_gap |
|---|---|---|
| 0.00 (ของเดิม) | 0.515 | 0.209 |
| 0.25 | 1.000 | 1.000 |

→ เป็น **บั๊กเชิงสถาปัตยกรรม ไม่ใช่เรื่อง tune weight** แก้ได้แน่นอน

### F2 — ⚠️ แต่ตัวเลข 1.000 นั้น "ปลอม" — metric มีช่องโหว่ ✅ ยืนยันแล้ว

แยกดูว่าคำตอบที่ถูกมาจากไหน (uniform / dev-01 / recall_frac 0.25):

```
ตอบถูกเพราะ item อยู่ใน context จริง :  12/163  ( 7.4%)
ตอบถูกเพราะ "pointer" เฉย ๆ         : 151/163  (92.6%)   <- ทิ้งเนื้อหาไปแล้ว เหลือแค่ id
```

**ต้นตอ:** D-13 ให้เครดิตการตอบผ่าน `source_item_ids` โดย**ไม่คิดค่าใช้จ่ายเลย**
`S` มี item ขนาด 100 token ที่แบก `source_item_ids` ได้ถึง **108 ids** (≈0.93 token ต่อ 1 id)
= ทิ้งของแล้วเก็บใบเสร็จไว้ แล้วเคลมว่า "จำได้"

**นี่คือปัญหาที่ต้องแก้ก่อนอย่างอื่นทั้งหมด** ถ้าไม่แก้ แล้วไปแก้ F1 ตัวเลขจะพุ่งเป็น 1.000
แล้วเราจะประกาศชัยชนะบนกระดานคะแนนที่พัง — ซึ่งผิดกฎข้อ §15.7 ของเอกสารหลักโดยตรง

**ทางแก้ที่ตรงกับเอกสารหลัก:** §4.1 บอกว่า `Page fault → retrieval miss` อยู่แล้ว
→ การ dereference pointer ควรเป็น **page fault ที่ต้องจ่าย token** เพื่อโหลด raw event กลับมา
ทำแบบนี้ทั้งถูกต้องตามทฤษฎี OS และทำให้ metric ซื่อสัตย์ไปพร้อมกัน

### F3 — KC4 อาจวัดคนละเรื่องกับที่ thesis อ้าง ⚠️ ยังไม่ยืนยัน ต้องตรวจต่อ

`ground_truth_utility` นับจาก "item ไหนเป็นหลักฐานล่าสุดของ fact"
แต่ใน generator การ revise fact เกิดจาก `fact_revision_rate` แบบสุ่ม — ไม่ผูกกับ surprise เลย
→ item ที่ถูกถามหาจริง มัก**เป็น item ที่ surprise ต่ำ** (เพราะเป็นการยืนยันซ้ำครั้งที่ N)

แปลว่า benchmark กำลังถามว่า *"หลักฐานล่าสุดคืออะไร"*
ส่วน thesis อ้างว่า *"สิ่งที่น่าประหลาดใจควรถูกจำ"* — **สองอย่างนี้คนละเรื่องกัน**

ต้องตัดสินว่า:
- (ก) benchmark ออกแบบผิด ไม่ได้ทดสอบ thesis → ต้องแก้ generator
- (ข) thesis ผิดสำหรับ workload แบบนี้จริง → เป็นข้อค้นพบที่มีค่า ต้องรายงาน

**ห้ามข้ามข้อนี้** เพราะ §7.4 บอกชัดว่าถ้า surprise ไม่ correlate กับความสำคัญจริง = สมมติฐานหลักผิด

### F4 — KC3 ช้าเพราะยังไม่ optimize ✅ ทราบสาเหตุแล้ว

`workingset.allocate()` และ `s_soma.on_query()` เป็น pure Python วนลูปทั้ง store
`allocate()` ที่ 10,000 items ใช้ ~1,200–1,500 ms (งบคือ 4 ms)
ยังไม่เคย optimize เลยสักครั้ง — และ §12 ห้าม optimize ก่อนมีตัวเลข ตอนนี้มีตัวเลขแล้ว จึงทำได้

---

## 3. แผนงานรอบถัดไป

> **ลำดับสำคัญมาก** WP-11 ต้องเสร็จก่อน WP-12 เสมอ
> ไม่งั้นจะได้ตัวเลขสวยปลอม ๆ จากกระดานคะแนนที่ยังพังอยู่

### WP-11 — ปิดช่องโหว่ metric (page-fault cost) 🔴 ทำก่อนเพื่อนเลย
- [ ] เสนอแก้ D-13 → ให้การตอบผ่าน `source_item_ids` ต้องจ่าย token (page fault)
- [ ] **ต้องให้ Nin อนุมัติก่อนแก้** เพราะ D-13 เป็น decision ที่ล็อกไว้แล้ว (ห้ามเปลี่ยนเงียบ)
- [ ] implement: ถ้า required item อยู่แค่ใน pointer → ต้องมีงบเหลือพอโหลด raw กลับมา ถึงจะนับว่าตอบได้
- [ ] เพิ่ม metric `page_fault_rate` และ `answered_via_pointer_rate` เข้า JSONL schema
- [ ] เพิ่ม test: policy ที่เก็บแต่ pointer ต้อง **ไม่** ได้ recall สูง
- [ ] รัน pilot ซ้ำ ดูว่าตัวเลขฐาน (ก่อนแก้ F1) เปลี่ยนไปแค่ไหน — คาดว่า `S` จะ**ตกลง** จาก 0.31

### WP-12 — แก้ retrieval ordering ใน policy S 🟠
- [ ] เพิ่ม `recall_budget_fraction` เป็น config (ค่าเริ่มต้นเสนอ 0.25–0.5)
- [ ] `on_query`: ให้ targeted recall ได้สิทธิ์เลือกก่อนในโควตาของตัวเอง แล้วค่อยให้ working set เติมส่วนที่เหลือ
- [ ] tune บน **dev seed เท่านั้น** (D-10) — ห้ามดู holdout ระหว่าง tune
- [ ] เพิ่ม test ว่า working set ต้องไม่กินเกินโควตาที่กำหนด
- [ ] วัดผลบน holdout **ครั้งเดียว** หลัง tune เสร็จ

### WP-13 — ตรวจสอบ KC4 / ความสอดคล้องของ benchmark กับ thesis 🟠
- [ ] วัด: item ที่ถูก required จริง มี surprise เฉลี่ยเท่าไหร่ เทียบกับ item ทั่วไป
- [ ] ตรวจว่า `fact_revision_rate` ทำให้ "หลักฐานล่าสุด" กลายเป็น item ที่ surprise ต่ำจริงไหม
- [ ] ตัดสินระหว่าง (ก) แก้ generator ให้ผูก revision กับ surprise / (ข) ยอมรับว่า thesis ไม่ตรงกับ workload นี้
- [ ] **ถ้าเลือก (ก) ต้องระวังมาก** — การแก้ generator ให้ผลออกมาดีขึ้น = การออกแบบข้อสอบเอง
      ต้องบันทึกเหตุผลลง `plans/CHANGELOG_REGIMES.md` พร้อมวันที่ (ตาม WP-02 §5.2)
- [ ] ถ้าเลือก (ข) → รายงานตรง ๆ ว่าเป็น kill criterion ที่เป็นจริง

### WP-14 — แก้ปัญหาความเร็ว (KC3) 🟡
- [ ] profile หาจุดที่ช้าจริง (คาดว่าอยู่ที่การวน scoring ทุก item ทุกครั้ง)
- [ ] vectorize การคำนวณ retention ด้วย numpy (numpy อยู่ใน dependency ที่อนุญาตแล้ว)
- [ ] ทำ index ให้ `on_query` แทนการ scan ทั้ง store
- [ ] เป้า: `allocate()` ที่ 10,000 items ให้ p95 ≤ 4 ms
- [ ] ต้องมี test ยืนยันว่าผลลัพธ์**เหมือนเดิมทุกประการ**หลัง optimize (ห้ามเปลี่ยนพฤติกรรม)

### WP-15 — รันเต็มสเกล 🟡
- [ ] รัน `phase0.json` เต็ม (7 regime × 4 budget × 3 tau × 11 seed)
- [ ] ประเมินเวลาก่อนรัน — pilot ใช้ 3.5 นาทีที่ 600 rows, ของเต็มใหญ่กว่ามาก
- [ ] เก็บผลไว้เป็น baseline ที่อ้างอิงได้

### WP-16 — สรุปและตัดสินใจ (Nin เป็นคนตัดสิน ไม่ใช่ agent) 🔵
- [ ] อ่านตัวเลขหลังแก้ครบแล้ว
- [ ] ตัดสิน: ผ่าน gate → ไป Phase 0.5 / ไม่ผ่าน → หยุดตาม §7.4
- [ ] ถ้าหยุด: เขียนบันทึกว่าเรียนรู้อะไร (มีค่ากว่าการฝืนทำต่อ)

---

## 4. กฎที่ต้องรักษาตลอด (ย้ำจากเอกสารหลัก)

- ❌ **ห้ามแก้ threshold / weight / regime config เพื่อให้ผ่าน gate** (§15.7)
- ❌ **ห้าม tune แล้ววัดบน holdout ซ้ำ ๆ** — tune บน dev, วัด holdout ครั้งเดียว (D-10)
- ❌ **ห้ามสร้าง** `kernel/`, `registry/`, `cortex/`, `modelbus/`, `trace/`, `packs/` (§12)
- ❌ ห้ามเพิ่ม dependency นอก stdlib + numpy โดยไม่ถาม
- ⚠️ ถ้าจะแก้ decision ที่ล็อกแล้ว (D-01..D-13) ต้องขออนุมัติก่อน ห้ามแก้เงียบ ๆ
- 🚨 สัญญาณว่า harness พัง ไม่ใช่ policy เก่ง: `competitive_ratio > 1.0`, `B0 < 1.0`,
  `S` ชนะใน `adversarial_flat`, หรือ test ผ่านหลังแก้ config ในคอมมิตเดียวกัน

---

## 5. สรุปสั้นที่สุดสำหรับรอบหน้า

> เครื่องมือเสร็จหมดแล้ว ปัญหาตอนนี้มี 3 ชั้น เรียงตามลำดับที่ต้องแก้:
> **(1) กระดานคะแนนพัง** — ให้เครดิตการทิ้งของแล้วเก็บใบเสร็จ (WP-11)
> **(2) ตัว S ดึงของกลับมาไม่เป็น** — working set กินงบหมด (WP-12)
> **(3) ข้อสอบอาจไม่ตรงกับสิ่งที่อ้าง** — surprise vs หลักฐานล่าสุด (WP-13)
>
> แก้ (1) ก่อนเสมอ ไม่งั้น (2) จะให้ตัวเลข 1.000 ที่ไม่มีความหมาย
